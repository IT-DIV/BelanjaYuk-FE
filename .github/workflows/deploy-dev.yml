name: Deploy Client Development

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore BelanjaYuk.Client.csproj

      - name: Build
        run: dotnet build BelanjaYuk.Client.csproj --configuration Release --no-restore

      - name: Publish
        run: dotnet publish BelanjaYuk.Client.csproj --configuration Release --output ./publish

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS Dev
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          port: 22
          script: |
            cd /root/BelanjaYuk_Client_Dev
            git pull origin dev
            docker-compose -f docker-compose.dev.yml down
            docker-compose -f docker-compose.dev.yml build --no-cache
            docker-compose -f docker-compose.dev.yml up -d
            docker system prune -f
